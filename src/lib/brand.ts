/** Static brand mark under /public (also mirrored as `src/app/icon.png` for favicon). */
export const BRAND_LOGO_PATH = "/brand/never-quit-punjabi-logo.png";

/** Home page hero background (full-bleed, dimmed in layout). */
export const HOME_HERO_IMAGE_PATH = "/brand/home-hero-golden-temple.jpg";
